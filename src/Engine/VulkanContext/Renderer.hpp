#pragma once

#include <vulkan/vulkan.h>

class Renderer {
public:
private:
public:
    Renderer();
    ~Renderer();
    Renderer(const Renderer&) = delete;
    Renderer& operator=(const Renderer&) = delete;
private:
};
